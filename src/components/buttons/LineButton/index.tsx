import React from "react";

import { LineButtonContainer } from "./styles";

export const LineButton: React.FC = ({ children }) => {
  return <LineButtonContainer>{children}</LineButtonContainer>;
};
