import styled from "styled-components";

export const LineButtonContainer = styled.button`
  background: transparent;
  border: none;
  cursor: pointer;
  font-size: 1rem;
  font-weight: 600;
  padding: 8px 16px;
  border-radius: 9999px;
  &:hover {
    background: #eee;
  }
`;
